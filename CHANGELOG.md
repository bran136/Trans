# 更新日志

本项目的正式版本变更记录在此文件中。版本号采用 `主版本.次版本` 格式，
Git 标签在版本号前添加 `v`，例如版本 `1.0` 对应标签 `v1.0`。

## [1.3] - Unreleased

### 离线听书

- 单次服务器固定和本机下载上限提高至 300 章，并支持按章节范围批量选择，便于分段缓存长篇书籍。
- “固定并下载到本地”执行期间阻止设备自动息屏，任务结束或关闭页面后自动恢复。
- 服务器固定状态增加持久快照，本机播放包增加轻量元数据索引，避免重复扫描大量音频；旧缓存会自动校准。
- TTS 缓存统计快照会持久保存，服务重启后先读取快照并在后台校准。

## [1.2] - 2026-08-11

### 连续播放与离线听书

- 在线播放和离线下载统一使用带句子时间轴的 AAC/M4A 播放包，不再向客户端发送不足 5 秒的短音频。
- 增加自适应预加载和下一章缓冲，倍速调整为 0.8、1、1.2、1.5、2 五档；首次播放等待缓冲，准备期间可以暂停，媒体加载超时为 3 分钟。
- 服务端继续按单句生成和复用语音，再按章节建立播放包；达到时长门槛的单句只保存索引，不重复占用音频空间。
- 本机离线缓存改为保存完整播放包，支持校验、六路断点续传、任务监控、取消和手动重试；缓存完整时播放音频不再消耗网络流量。
- 定时暂停按句末生效；浏览器允许网页调节媒体音量时会在结束前渐弱，否则直接暂停。
- 修复离线状态和缓存复用问题；普通缓存与固定内容分开统计实际磁盘占用，统计改用后台快照以加快监控和听书页面加载；章节总句数改用持久索引，并提供旧 WAV 缓存迁移脚本。
- 优化了部分界面的显示效果。

## [1.1] - 2026-08-06

### 离线听书

- 新增按章节管理的离线听书缓存，支持固定服务器缓存、下载到当前浏览器、删除本机缓存和取消服务器固定。
- 服务器缓存任务最多并行处理两个章节，每章内部严格逐句处理，并支持取消正在进行的任务。
- 缓存状态显示服务器与本机的已缓存句数、总句数和容量，并显示浏览器存储额度与持久化状态。
- 听书优先读取当前浏览器的 IndexedDB 缓存，弱网环境下减少重复加载音频产生的流量。
- 服务器会复用相同朗读配置下已有的音频，只调用 MiMo 生成缺失句子。

### 改进与修复

- 页面刷新后重新进入缓存管理时，会恢复服务器任务监控并继续显示已处理、复用、生成和失败句数。
- 优化手机端离线缓存页面、任务消息、按钮布局和滚动行为。
- 修复手机端 MiMo 控制台按钮文字没有垂直居中的问题。
- 修复手动选择相邻句子时，高亮被当前播放句覆盖并引起页面跳动的问题。
- 修复重复点击取消、章节缓存统计及已有音频复用等边界问题。

## [1.0] - 2026-07-23

Trans 的首个正式版本。

### 主要功能

- 提供共用密码登录入口，以及相互独立的在线翻译和在线阅读模块。
- DeepSeek 服务端代理、谷歌翻译浏览器直连、多引擎对比、翻译历史与持久化缓存。
- TXT、EPUB、PDF 导入，书架管理、章节阅读、阅读进度、主题和字体设置。
- Xiaomi MiMo TTS 听书、音频缓存、余额状态和 Cookie 管理。
- 移动端阅读适配、Web App Manifest、背景音频播放和字体离线缓存。

### 安全与配置

- 提供 Session、CSRF、登录失败限制、上传限制和服务端请求地址校验等安全措施。
- 密码和 API Key 保存在 `.env`，运行数据和敏感配置默认不纳入 Git。
- 首次运行时需要复制 `.env.example` 为 `.env`，并设置强 `APP_PASSWORD`。
- `SECRET_KEY` 可以留空，由程序自动生成并保存到私有配置目录。

### 版本与缓存

- 页面显示“发布版本 + 自动构建指纹”，例如 `v1.0+a07a6f8b`，便于确认当前加载的页面版本。
- CSS、JavaScript、模板、图标和 Manifest 内容变化后，刷新页面即可看到新的构建指纹。
- 静态资源 URL 使用内容哈希自动更新，避免浏览器继续使用旧缓存。
- 版本角标提供 GitHub 项目入口。

[1.3]: https://github.com/bran136/Trans/releases/tag/v1.3
[1.2]: https://github.com/bran136/Trans/releases/tag/v1.2
[1.1]: https://github.com/bran136/Trans/releases/tag/v1.1
[1.0]: https://github.com/bran136/Trans/releases/tag/v1.0
